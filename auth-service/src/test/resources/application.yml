spring:
  datasource:
    url: jdbc:h2:mem:auth_service;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

equipmap:
  auth:
    jwt:
      secret: test-secret-test-secret-test-secret-test-secret
    cookie:
      secure: false
