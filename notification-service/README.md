# notification-service

Serviço de notificações do EquipMap MVP.

## Local

```powershell
.\gradlew.bat bootRun
```

Swagger: `http://localhost:8088/swagger-ui.html`

## Docker Compose

```powershell
.\gradlew.bat bootJar
docker compose up --build
```
